/**
 * KpiCard — re-export from shared PortalKpiCard for backward compatibility.
 * Manager code that imports KpiCard directly will still work.
 * New code should import from '@/components/portal/PortalKpiCard' directly.
 */
export { default } from "@/components/portal/PortalKpiCard.jsx";
